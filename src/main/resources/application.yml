server:
  port: 8080

spring:
  application:
    name: temple-donation-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-mute-cherry-ax35xw5n-pooler.c-4.us-east-2.aws.neon.tech:5432/neondb?sslmode=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_gOCSIRvJMX37}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: warn

app:
  jwt:
    secret: ${JWT_SECRET:temple-donation-app-dev-secret-key-change-me-0123456789}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:1440}